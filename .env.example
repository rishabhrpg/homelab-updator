# Webhook Configuration
# Copy this file to .env and update with your values

# GitHub webhook secret (set the same in GitHub webhook settings)
SECRET=your-webhook-secret-key

# Port for the webhook server
PORT=8000

# Deploy Script Path (optional - defaults to ./deploy-script.sh)
# Uncomment and set custom path if your deploy script is elsewhere
# DEPLOY_SCRIPT=/home/super/home-lab/local-chat/deploy.sh
